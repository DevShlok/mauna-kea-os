import { db } from "./src/db"; import { candidates } from "./src/db/schema"; import { ilike } from "drizzle-orm"; async function main() { const cands = await db.select().from(candidates).where(ilike(candidates.name, "%Ravi Shankar%")); console.log("Found:", cands.length, "candidates"); cands.forEach(c => { console.log(`ID: ${c.id}, Name: ${c.name}, Company: ${c.company}`); }); process.exit(0); } main();
